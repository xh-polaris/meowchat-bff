syntax = "v1"

info(
    title: "common api struct"
)

type Status {
    code int `json:"code"`
    msg string `json:"msg"`
}

type UserPreview {
    userId string `json:"userId"`
    nickname string `json:"nickname"`
    avatarUrl string `json:"avatarUrl"`
}
